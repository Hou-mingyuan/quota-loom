# 安全政策

## 支持范围

项目仅为最新正式版本提供安全更新。尚未发布正式版本时，`main` 分支代表当前支持状态。

| 版本         | 安全更新 |
| ------------ | -------- |
| 最新正式版本 | 支持     |
| 更早版本     | 不支持   |

## 私下报告漏洞

请使用仓库 **Security** 页面中的 **Report a vulnerability** 私下提交 GitHub Security Advisory。不要创建公开 Issue，也不要附加真实会话日志或应用数据库。

报告应尽量包含：

- 受影响版本和操作系统
- 漏洞影响与攻击前提
- 最小复现步骤或概念验证
- 建议的缓解方式（如有）

维护者会尽快确认收到报告，并在能够安全披露后协调修复和公告。请在修复发布前避免公开细节。

## 敏感数据提醒

此应用读取本地 AI 编码会话目录。安全报告中不得包含提示词、回复内容、API Token、用户名、会话数据库或未脱敏的本地路径。必要时请构造最小的虚假 JSONL 样本。

## 上游依赖公告

CI 使用 npm Audit、RustSec 和 CodeQL。RustSec 当前会报告来自 Tauri Linux GTK3 栈及 Tauri 工具链的若干“未维护”提示，以及 `glib::VariantStrIter` 的健全性提示；它们都是间接依赖，无法由本项目独立升级。项目不会静默忽略这些公告，并会随 Tauri 上游迁移持续复查。可利用漏洞仍会导致安全检查失败。
